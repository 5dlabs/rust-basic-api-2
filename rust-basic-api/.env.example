# Example configuration values; replace placeholders with real settings
POSTGRES_SCHEME=postgresql
DB_USER=<USER>
DB_PASSWORD=<PASSWORD>
DB_HOST=localhost
DB_PORT=5432
DB_NAME=<DATABASE>
DATABASE_URL=${POSTGRES_SCHEME}://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}
SERVER_PORT=3000
RUST_LOG=info
