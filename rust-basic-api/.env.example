# Example configuration values; replace placeholders with real settings
POSTGRES_SCHEME=postgresql
DB_USER=example_user
DB_SECRET=example_secret
DB_HOST=localhost
DB_PORT=5432
DB_NAME=example_db
DATABASE_URL=${POSTGRES_SCHEME}://${DB_USER}:${DB_SECRET}@${DB_HOST}:${DB_PORT}/${DB_NAME}
SERVER_PORT=3000
RUST_LOG=info
