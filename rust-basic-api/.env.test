# Provide connection strings via environment variables before running tests.
# Example:
# TEST_DATABASE_URL=<postgres connection uri for tests>
# DATABASE_URL=<postgres connection uri for tests>
SERVER_PORT=3100
RUST_LOG=debug
DB_MAX_CONNECTIONS=5
DB_MIN_CONNECTIONS=1
DB_ACQUIRE_TIMEOUT_SECONDS=5
DB_IDLE_TIMEOUT_SECONDS=300
DB_MAX_LIFETIME_SECONDS=1200
DB_CONNECT_TIMEOUT_SECONDS=5
