//! Data models for the application live in this module.

// Concrete models will be introduced as features that require them are
// implemented. The module exists now so downstream work can immediately add
// types without additional scaffolding changes.
